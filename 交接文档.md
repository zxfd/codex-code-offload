# 交接文档：codex-code-offload 浏览器回归与修复收尾

更新时间：2026-08-14

## 目标与边界

- 仓库：`/Users/gin/gin-code/codex-code-offload`
- 真实回归使用 Chrome 扩展和仓库固定 Provider adapter。
- 文本和多模态请求均由程序化脚本控制；外部模型不读取 DOM、页面结构或失败 Provider 证据来决定下一步。
- 默认只提交、不推送；不写凭据和隐私。

## 已完成实现

1. 多模态媒体链路已落地：本地校验 PNG/JPEG/WebP，写入浏览器剪切板，固定 composer 聚焦后执行 `ControlOrMeta+V`，再以粘贴前基线、最长 120 秒上传等待和稳定 5 秒窗口确认就绪。
2. ChatGPT、Gemini、Qwen 均已接入统一 `pasteProviderImages`；Qwen/Gemini 采用“先填提示词、再粘贴图片、最后发送”的适配器流程。
3. Gemini 发送增加固定“发送/Send”按钮优先和一次 Enter 提交重试，并要求观察到固定用户消息或 composer 清空后才进入回答等待。
4. runner 不再把 UI/DOM 证据拼接到下一个 Provider 的 prompt；失败证据只在本地生成并清理。
5. ChatGPT 长文本两步粘贴已由真实 Chrome 直接验证；当首个 Provider 导航失败时，固定路由仍可回退并返回完整结果。
6. 剪切板写入/粘贴和 Gemini/Qwen 提交均有 2 次重试；长文本输入采用至少 5 秒稳定等待，长内容采用 10 秒稳定等待。

## 真实 Chrome 回归证据

### ChatGPT

- 多模态：通过。`attachmentsReady=true`，2 张图片均被粘贴，回答返回 `400x400` 和 `600x300`。
- 长文本：通过。两步上下文/指令链路返回 `LONG_TEXT_CHATGPT_OK`。

### Qwen

- 多模态：通过附件链路。调整为先填提示词后粘贴后，`attachmentsReady=true`，回答确认 `IMAGE_COUNT=2`。
- 视觉尺寸回答返回 `300x300` 和 `450x200`，与源文件像素尺寸不一致；因此只确认图片已进入模型和模型有回答，不把视觉尺寸精度记为通过。

### Gemini

- 纯文本：通过，返回 `GEMINI_TEXT_OK`。
- 单图片和双图片：剪切板附件计数均有增长；修复 composer 稳定等待和两次提交容错后，双图片端到端真实通过，`attachmentsReady=true`，Gemini adapter 返回成功回答。
- 结论：此前失败是过严的 composer 长度校验导致的提前失败，不是 Gemini OSS 或模型响应不可用。

### ChatGPT 发送后误 fallback 修复（2026-08-14）

- 真实观察到一次 ChatGPT 已发送、页面可能继续生成时出现“请求过于频繁”弹窗；旧流程在 answer wait 阶段直接记为失败，并转到 DeepSeek。对应 runner 记录为 `ambiguous_post_send`，不能把最终返回的 `PROVIDER_OK:ChatGPT` 误当作 ChatGPT 成功，因为当次实际 `providerId` 是 `deepseek-expert`。
- 当前 adapter 在发送前记录 assistant 消息基线，只接受发送后新增的 `[data-message-author-role="assistant"]` 消息；不再使用主区最后一段文本作为旧回答兜底。
- 发送点击超时按“已开始但结果不确定”处理：不重复点击发送；限流弹窗在发送后属于软中断，优先关闭，无法立即关闭也继续观察同一请求，直到新增回答完成或真实超时。
- 发送后的未确认失败不会写入负面 health cache；旧版负缓存因协议版本不匹配自动失效。成功 receipt 增加 `responseConfirmed=true` 和 `responseConfirmation=new_assistant_message`。
- 真实复验（当前代码、全新运行会话）：`chatgpt-exact-smoke-current-code-20260814` 返回 `providerId=chatgpt-sol-xhigh`、`responseConfirmed=true`、`responseConfirmation=new_assistant_message`、`answer=PROVIDER_OK:ChatGPT`，未发生 fallback；事件日志同步记录 `response_confirmed=true`。
- 本地回归：`node --test skill/scripts/tests/chatgpt-response-confirmation.test.mjs`、两个 adapter `node --check`、`git diff --check` 均通过。

### 文本回退

- 默认文本路由真实通过：ChatGPT 首次导航遇到 CDP 10 秒导航超时后，固定回退到 DeepSeek，返回 `LONG_TEXT_OK`。

### ChatGPT 终局会话清理（2026-08-14）

- ChatGPT 终局回答确认后，固定执行“关闭限流弹窗（如有）→ 更多 → 归档 → 菜单关闭确认”，不刷新页面，完成确认后才关闭 Chrome 标签；`NEED_MORE_CONTEXT` 续聊不会提前清理。
- 归档前限流弹窗会按现有 acknowledgement 流程关闭并重试菜单步骤；弹窗无法关闭或归档菜单未关闭时，标记 `conversation_cleanup_failed` 并保留标签。
- ChatGPT 全流程的可恢复阻塞统一优先尝试关闭“明白了”限流弹窗；回答等待期间若 `停止回答`/`Stop generating` 可见，视为模型仍在生成，只继续等待，不执行弹窗恢复。
- Web-LLM runner 要求显式传入 `browserChannel: "chrome"`，并在非 Chrome 通道下发送前失败；禁止使用 `getForUrl()`/`getDefault()` 选择 Provider 浏览器。
- 用户 Chrome 真实回归确认：限流弹窗关闭后可继续输入和发送；归档后不刷新页面，当前会话链接即时消失，随后标签关闭；结构化结果为 `providerId=chatgpt-sol-xhigh`、`responseConfirmed=true`、`conversationCleanup.verification=sidebar_link_absent_without_reload`、`remainingTabKeys=[]`。

### 模型路由与技能入口（2026-08-14）

- `$luna-model-routing` 的 canonical 入口是 `${HOME}/.agents/skills/luna-model-routing/SKILL.md`，本仓库源文件是 `skills/luna-model-routing/SKILL.md`；不得误读 `${HOME}/.codex/skills/luna-model-routing/SKILL.md`。
- `luna-model-routing` 将有界低风险编码映射到 Spark；`codex-model-routing-team` 当前的自动路由白名单只有 Luna/Sol。若用户明确要求 Spark，复用 routing-team 的项目 Thread 定位、健康探针、任务包、读取、核验和归档生命周期，但不能使用 `fork_thread` 继承当前窗口历史。
- 本轮 project Thread 创建接口对多个已定位项目均返回参数错误；按 routing-team 规则未使用 projectless 绕过，代码由主任务完成并保留该阻塞证据。

## 旧目录清理状态

以下旧目录已在三家多模态真实通过后从原路径移出并隔离：

- `/Users/gin/.local/share/codex-agentchat-offload`
- `/Users/gin/.codex/backups/agentchat-code-offload-*`

## 后续唯一动作

若需要把三家多模态都记为完整通过，应先在 Chrome 中恢复 Gemini 图片请求的模型响应或更换可用会话，然后使用现有固定入口重跑 Gemini 单图和双图；ChatGPT 本次误 fallback 修复已完成，在出现新的程序错误前不再扩大浏览器探测范围。
