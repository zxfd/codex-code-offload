# 交接文档：通用未知网页低上下文摄取与 Web‑LLM 理解

更新时间：2026-08-15
仓库：`/Users/gin/gin-code/codex-code-offload`
起点提交：`371501e feat(qwen): 固化终局对话归档清理`

## 给新会话窗口的任务

实现一个**通用未知网页摄取层**。它不是抖音页面适配器，也不能为某个网站硬编码 selector；抖音创作中心只作为真实验收样本。

最终必须实现这个分层：

```text
Codex 模型
  只发出：URL、任务目标、允许的处理策略
          ↓
本地页面摄取程序 / Browser Adapter
  在本机内存中提取、截图、扫描、遮盖，写入临时文件
          ↓
Web‑LLM
  接收临时文本或临时图片，返回短结论
          ↓
Codex 模型
  只接收：类型信号 + 短结论 + 回执
```

“低上下文”是硬边界：完整正文、原始 DOM、`innerHTML`、无裁剪截图、OCR 全文、Cookie/存储和无关私密数据，均不得出现在 Codex 的工具输出或模型上下文。它们只可短暂存在于本机受控进程和 `0700` 临时目录，之后必须清理。

## 开始前必读与执行方式

1. 读取 `skills/luna-model-routing/SKILL.md`、`references/routing-matrix.md`、`references/communication-contract.md`。
2. 读取 `skill/SKILL.md`，尤其是 `LOCAL_PREPROCESS_TO_WEB`、`WEB_TEXT`、`WEB_MULTIMODAL`、图片隐私边界和 Browser Adapter 规则。
3. 检查并复用：
   - `skill/scripts/web-provider-runner.mjs`
   - `skill/scripts/provider-utils.mjs`
   - `skill/scripts/media-upload.mjs`
4. 先检查 `git status --short --branch`，保留已有用户改动，不推送。

这是代码实现任务。按当前 `luna-model-routing`：先创建一个 `gpt-5.3-codex-spark`、`medium` 的项目 Thread 负责源代码实现；主窗口只做任务包、窄范围验收、真实浏览器验收、集成和提交。不要让 Spark 或 Web‑LLM 执行外发、删除、发布、账号修改或推送。

## 既有能力与不可误用的边界

可复用：

- `runProviderFallback(...)`：受控 `promptPath`，按 `providers.json` 的文本或多模态链路执行。
- `imagePaths` + `pasteProviderImages(...)`：受控 PNG/JPEG/WebP 粘贴与附件就绪确认。
- `makeAttemptPrompt(...)` / `removePrompt(...)`：短生命周期的受控临时 prompt。
- Provider Adapter 已有新会话、回答确认、续聊、健康缓存与终局标签清理。

不可误用：

- `captureSemanticUiEvidence(...)` 仅用于 Provider 失败时的白名单控件诊断，不能变成通用网页正文抓取器。
- 不把 `domSnapshot()`、`innerHTML` 或截图通过 `nodeRepl.write`、终端输出或日志送进 Codex。
- 不为 `creator.douyin.com` 单独写实现；验收代码可以使用通用的“第一张可见卡片/语义字段”策略，但核心能力必须适用于任意单个已授权 URL。

当前缺口正是“任意 URL → 本地摄取/脱敏 → Web‑LLM”的通用入口。

## 必须实现的能力

### 1. 受控入口与有界回执

调用 Codex 只允许传入：

```js
{
  url,
  objective,
  processingPolicy: {
    allowText: true,
    allowVisual: true,
    allowExternalTransfer: false, // 默认 false
    maxImages: 4,
  },
}
```

要求：

- URL 必须是用户明确提供或已批准的单个 URL；拒绝 URL 列表、通配符、跨域扩展和隐式跳转。
- 固定 Browser Adapter 打开新的任务隔离标签；不得扫描或接管其他用户标签。
- URL 中的 token/完整 query、Cookie、localStorage、原始页面源代码、完整可见文本均不得进入 Codex 输出或事件日志。
- 返回 Codex 的 `pageSignals` 只能包含有界元数据：域名、加载状态、可见字符数、图片/Canvas/表格数量、候选模态、风险等级、内容哈希、临时工件清理结果。

建议的回执等价结构：

```js
{
  status: 'completed' | 'blocked' | 'requires_user_approval' | 'failed',
  requestId,
  origin,
  modality: 'text' | 'multimodal',
  pageSignals: { visibleTextChars, imageCount, canvasCount, tableCount, riskLevel, contentSha256 },
  externalTransfer: { provider, model, attachmentsReady } | null,
  result: {}, // 有界结论/字段，不含完整原文
  cleanup: { captureArtifactsRemoved, providerTabCleanup },
}
```

### 2. 文字摄取：在本地直接进入 Web‑LLM

分类、提取、去重、截断和敏感扫描必须由本地 Adapter 完成，不能让 Codex 先读整页再选择路线。

- 页面执行上下文只提取可见主体文字、标题层级和可访问表格内容；排除脚本、隐藏节点、导航噪音和无关重复区域。
- 提取结果直接写入受控临时 `prompt.txt`，不得打印到工具输出。
- 本地确定性扫描：凭据、token、Cookie、私钥、邮箱/电话/身份证格式等。高风险命中时默认阻断外发，只返回类别与数量。
- 长文本在本地完成长度限制、去重和结构化截断；不要把全文交给 Codex 进行摘要。
- 文字路线调用 `runProviderFallback(... requestMetadata: { modality: 'text' })`。

### 3. 视觉摄取：在本地截图、检查、遮盖后再外发

只有像素本身影响结论时才选择视觉路线，例如 Canvas、图表、图像文字、复杂表格、多栏布局、难 OCR 或视觉层级判断。

- 截图字节直接写入受控临时 PNG/JPEG/WebP；不得作为工具图片内容返回 Codex。
- 截取任务相关区域，最多 4 张，不得为方便而整页外发。
- 本地隐私检查至少包括：敏感域名/路径策略、OCR/规则扫描、二维码/登录验证码/身份证件/支付/医疗风险检测。
- 能确定的敏感区域先本地遮盖；不能可靠判断时默认返回 `requires_user_approval`，不外发。
- 视觉路线调用 `runProviderFallback(... requestMetadata: { modality: 'multimodal' }, imagePaths)`，并要求 `attachmentsReady === true`。

### 4. 外发授权、最小化与清理

- `allowExternalTransfer` 默认 `false`。本地摄取与临时工件生成可完成，但不得打开 Provider 请求。
- 对来自已登录页面、创作数据、个人信息或图片的内容，在**实际外发前**必须向用户确认：具体发送哪些数据类别，以及将发送到哪个当前 Provider。不能把本交接或历史泛化授权当作本次外发授权。
- Provider 不得获得 DOM、selector、浏览器控制权、Cookie、文件系统或执行权限；外部回答是未验证假设，必须本地核验。
- 成功、失败、阻断、超时和续聊终止都要清理原始正文、截图、OCR 中间件和遮盖前图片。只保留请求 ID、域名、哈希、大小、风险类别、Provider、模型、模态、时间与清理结果；禁止在 `events.jsonl` 保存源内容。
- `NEED_MORE_CONTEXT` 仅可走既有受控继续协议，不能重新导出整页或扩大页面范围。

## 非目标

- 不实现抖音专用 adapter，不硬编码本样本的标题、指标或 selector。
- 不退化为“先输出 `domSnapshot()` 给 Codex，再手动决定文字/截图”。
- 不让 Web‑LLM 根据 DOM 或页面结构决定点击、导航、跟踪动作。
- 不读取浏览器历史、Cookie、Local Storage，不点击导出，不发布、编辑、删除、置顶、下载任何抖音内容。
- 不推送。只提交本任务文件，使用中文 Conventional Commit。

## 真实验收样本：抖音创作中心第一条视频

验收 URL：

```text
https://creator.douyin.com/creator-micro/content/manage
```

### 已验证的冻结基线

读取时间：2026-08-15（Asia/Shanghai）。页面已登录，位置为“内容管理 → 作品（39）→ 全部”。首条可见作品是置顶作品：

| 字段 | 冻结值 |
|---|---|
| 置顶状态 | `置顶` |
| 标题开头 | `【黄金量化实验】第10天：我发现99%的EA都会死` |
| 时长 | `01:31` |
| 发布时间 | `2026年06月29日 20:05` |
| 发布状态 | `已发布` |
| 播放（原始展示） | `12万` |
| 点赞 | `1442` |
| 评论 | `452` |
| 分享 | `658` |
| 收藏 | `588` |
| 完播率 | `7.02%` |
| 2 秒跳出率 | `37.37%` |
| 吸粉量 | `970` |

播放和互动数据会变动，因此验收规则是：

1. **样本身份硬断言**：首条卡片必须匹配置顶状态、标题开头、时长、发布时间、已发布状态。任一不匹配时停止并报告“样本身份变化”，不得改用第二条。
2. **字段完整性断言**：必须从同一张卡片采集播放、点赞、评论、分享、收藏、完播率、2 秒跳出率、吸粉量共 8 项，保留原始展示字符串和可选规范化数值。
3. **动态指标比对**：逐项比较冻结值并记录差异，但不因播放/互动增长判失败。
4. **低上下文断言**：日志和 Codex 回执不得含完整口播文案、整页 DOM、整页截图或其他卡片数据；允许出现本表的有界字段，因为它们是用户明确指定的验收基线。

### 真实浏览器验收步骤

1. 用受控 Chrome 新建任务标签打开验收 URL。若未登录，停在登录状态，请用户在 Chrome 登录；不换浏览器、不读会话数据。
2. 等列表稳定后，仅在本地执行通用摄取和第一卡片身份核验，先输出不含正文的 `pageSignals` 与有界字段。
3. 先以 `allowExternalTransfer: false` 运行一次：必须证明文字临时工件、敏感扫描、有界回执和临时清理均正确，且没有 Provider 请求。
4. 准备把第一张卡片的最小化文本或截图发送给 Web‑LLM 前，向用户发起即时确认，明确数据范围（标题、发布时间、状态和 8 项创作数据，或经裁剪/遮盖的截图）与当前 Provider 目的地。
5. 得到确认后，至少完成文字路线。若通用分类把卡片判为视觉路线，也必须证明多模态附件就绪。Provider 的有界结果必须结构化复述标题开头、时长、发布时间、置顶/发布状态和 8 项指标。
6. 用本地已保存的最小事实核验 Provider 结论，确认捕获临时文件与截图/OCR 中间件删除；Provider 标签按既有终局规则清理。

## 必须覆盖的自动化测试

1. **文字不入 Codex**：长正文只进临时文件；回执/日志不含正文，只含计数、哈希、有界结果。
2. **视觉不入 Codex**：截图字节只写临时文件，未调用图片展示/发射接口；多模态仅把临时路径交给上传器。
3. **阻断优先**：token、二维码、高风险登录页返回 `requires_user_approval` 或 `blocked`，不创建 Provider 请求、不上传图片。
4. **临时文件清理**：成功、Provider 失败、超时、`NEED_MORE_CONTEXT` 的每种路径都有明确清理/保留语义和测试。
5. **输入边界**：拒绝多 URL、外部路径、过多/非图像文件、未授权跨域跳转、把原始 DOM 作为 prompt 的调用。
6. **Runner 集成**：文字走 `text`，视觉走 `multimodal`，且不破坏 Provider 顺序、健康缓存、回答确认和终局清理。

## 完成条件

- 交付的是通用未知网页摄取层，不是抖音特例。
- 大正文、像素和 OCR 全文未进入 Codex 上下文或可见工具输出。
- 默认不外发；敏感/不确定页面失败关闭。
- 本地测试证明文字、视觉、阻断、临时清理和 Runner 集成。
- 抖音样本完成真实 Chrome 本地摄取与身份核验；任何向 Web‑LLM 外发创作数据的动作都获得即时用户确认，并留存不含源内容的回执。
- `git diff --check`、针对性测试和适当的全量 `node --test` 通过。
- 只暂存本任务文件，提交中文 Conventional Commit，不推送。

## 可直接粘贴给新窗口的任务提示

```text
请在 /Users/gin/gin-code/codex-code-offload 实现交接文档.md 所述“通用未知网页低上下文摄取与 Web‑LLM 理解”。严格遵循 skills/luna-model-routing/SKILL.md：先由 gpt-5.3-codex-spark（medium）完成源代码实现，主 Agent 负责窄范围验收、真实浏览器验收和提交。不要只实现抖音；抖音创作中心 URL 仅是验收样本。完整正文、DOM、截图、OCR 全文不得进入 Codex 上下文，必须在本地 Adapter 内直接写入受控临时工件后交给 Web‑LLM。外发默认关闭；向 Web‑LLM 发送抖音创作数据前，必须在动作发生前向我确认具体数据和 Provider 目的地。完成后运行本地测试、真实 Chrome 验收、提交但不推送，并给出回执。
```
