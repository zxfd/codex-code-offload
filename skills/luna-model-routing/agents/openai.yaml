interface:
  display_name: "Luna Multi-Agent 路由"
  short_description: "用原生 subagents 隔离任务并优先把重推理交给 Web-LLM"
  default_prompt: "使用 $luna-model-routing：主 Agent保留执行与验收，独立探索交给原生 subagents，超过 token gate 的重推理优先交给 Web-LLM。"
